# Timeline from successor representation

A simple example of creating a compresset timeline from a set of successor representations (with a spectrum of time contants) via inverse Laplace transform. 
